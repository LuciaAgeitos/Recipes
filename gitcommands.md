# Git workflow to create a document and track it

git init - one time only for the "life" of the folder

nano "name".md - add doc for tracking

git status

git add

git commit -m

git status

git log
